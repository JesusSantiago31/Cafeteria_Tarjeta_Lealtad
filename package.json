{
  "name": "cafeteria-loyalty-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node -e \"const fs = require('fs'), { execSync } = require('child_process'); if (fs.existsSync('Frontend')) { execSync('npm --prefix Frontend install && npm --prefix Frontend run build', { stdio: 'inherit' }); } else { execSync('npm install && npm run build', { stdio: 'inherit' }); }\""
  }
}
